#include "Sema/Autoderef.h"
#include <cassert>

namespace rust_compiler::sema {

TyTy::BaseType *
Adjuster::adjustType(const std::vector<Adjustment> &adjustments) {
  assert(false);
}

} // namespace rust_compiler::sema
